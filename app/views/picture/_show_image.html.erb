

<% current_supplier.pictures.each do |picture| %>
<%= cl_image_tag(picture.public_id, :width => 200, :height => 200) %>


<% end %>

<%= form_for :picture ,url:profilepicture_path ,method:'put' do |f| %>
  <%= f.collection_radio_buttons :profilepicture , Picture.where(supplier_id: current_supplier.id) ,:supplier_id ,:public_id %>


  <%= f.submit 'Choose as profile picture', class: 'button is-primary' %>
  <% end %>
